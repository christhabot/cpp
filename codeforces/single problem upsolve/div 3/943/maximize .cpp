#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() 
{
    ll amountq, num, y;
    bool good;
    scanf("%lld", &amountq);
    while(amountq--) 
    {
        scanf("%lld", &num);
        printf("%lld\n", num-1);
    }
    return 0;
}
